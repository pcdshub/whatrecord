[build-system]
requires = [
    "cython",
    "epicscorelibs",
    "setuptools",
    "setuptools_dso",
    "wheel",
]

[project]
dependencies = [
    "aiohttp",
    "apischema[graphql]",
    "cython",
    "graphviz",
    "jinja2",
    "lark-parser",
]
